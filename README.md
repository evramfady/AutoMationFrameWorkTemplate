# AutomationFrameWorkTemplate

This repository contains an automation testing framework template (Maven, TestNG, Cucumber, Selenium).

How to run

Run headless with a specific browser:

```powershell
mvn "-DsuiteXmlFile=testng.xml" "-Dheadless=true" "-Dbrowser=chrome" test
```
